[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prstk-research"
version = "0.1.0"
description = "Reproducible Taiwan ETF strategy research platform"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
prstk-research = "prstk_research.pipeline:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
