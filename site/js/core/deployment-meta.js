const target=document.querySelector('.footer');if(target){try{const meta=await fetch('data/deployment.json').then(r=>r.json());const line=document.createElement('small');line.className='deployment-meta';line.textContent=`版本 ${meta.commit_sha||'—'} · 資料 ${meta.data_end_date||'—'} · 模型 ${meta.model_version||'—'} · 部署 ${meta.deployed_at||'—'}`;target.append(document.createElement('br'),line)}catch{}}
