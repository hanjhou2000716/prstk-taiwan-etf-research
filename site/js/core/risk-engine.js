function returns(rows){return rows.slice(1).map((row,index)=>Number(row.nav)/Number(rows[index].nav)-1)}
export function rollingRisk(rows,window=252){const output=[];for(let end=window;end<=rows.length;end++){const slice=rows.slice(end-window,end),r=returns(slice),average=r.reduce((a,b)=>a+b,0)/r.length,variance=r.reduce((a,b)=>a+(b-average)**2,0)/Math.max(1,r.length-1),growth=Number(slice.at(-1).nav)/Number(slice[0].nav);output.push({date:slice.at(-1).date,cagr:growth**(252/(slice.length-1))-1,volatility:Math.sqrt(variance*252),return:growth-1})}return output}
export function crisisMetrics(rows,start,end){const slice=rows.filter(row=>row.date>=start&&row.date<=end);if(slice.length<2)return{status:'unavailable',start,end,observations:slice.length};const values=slice.map(row=>Number(row.nav)),returnsList=returns(slice),peak=Math.max(...values),trough=Math.min(...values),first=values[0],last=values.at(-1),average=returnsList.reduce((a,b)=>a+b,0)/returnsList.length,variance=returnsList.reduce((a,b)=>a+(b-average)**2,0)/Math.max(1,returnsList.length-1);return{status:'available',start:slice[0].date,end:slice.at(-1).date,observations:slice.length,totalReturn:last/first-1,maxDrawdown:trough/peak-1,volatility:Math.sqrt(variance*252)}}
