export function maintenanceRatio(collateral,debt){return debt>0?collateral/debt:Infinity}
export function stressGrid({collateral,debt,ltv=.6,marginCall=1.3,rollover=1.66,release=1.67,declines=[0,.1,.2,.3,.4,.5,.6],ltvs=[.2,.3,.4,.5,.6]}={}){return ltvs.map(initialLtv=>({initialLtv,values:declines.map(decline=>{const principal=Math.max(debt,collateral*initialLtv);const value=collateral*(1-decline);const ratio=maintenanceRatio(value,principal);return {decline,ratio,loanToValue:principal/value,marginCall:ratio<marginCall,rollover:ratio>=rollover,release:ratio>=release,shortfall:Math.max(0,marginCall*principal-value)}})}))}
