const report=await fetch('data/reconciliation_report.json').then(r=>r.json()).catch(()=>({validation_layers:{}}));const section=document.createElement('section');section.className='section';section.innerHTML='<div class="section-title"><h2>分層驗證狀態</h2><p>格式通過不等於研究結論已驗證</p></div><div id="validationLayers" class="stack-list"></div>';document.querySelector('main').insertBefore(section,document.querySelector('footer'));const labels={source_integrity:'來源完整性',schema_validation:'資料結構',corporate_action_validation:'公司行動',backtest_engine_validation:'回測引擎',lookahead_validation:'前視偏誤',metric_validation:'指標公式',python_js_parity:'Python／JavaScript 一致性',broker_assumption_validation:'券商條件',publish_readiness:'正式發布準備'};const box=document.querySelector('#validationLayers');Object.entries(report.validation_layers||{}).forEach(([key,value])=>{const item=document.createElement('div');item.className='stack-item';const name=document.createElement('strong');name.textContent=labels[key]||key;const state=document.createElement('span');state.textContent=value;state.className=`status ${value==='passed'?'good':value==='not_run'||value==='pending_required_layers'?'warn':'danger'}`;item.append(name,state);box.append(item)});
